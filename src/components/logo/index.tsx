import { TouchableOpacity } from "react-native";
import { Logo } from "./styles";

export const MyLogo: React.FC = () => {
  return (
    <TouchableOpacity>
      <Logo>TDA</Logo>
    </TouchableOpacity>
  );
};
